<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:background="@drawable/iis_background_image"
    tools:context=".NetworkingFragment"
    android:id="@+id/relLay_NetHome">

<TextView android:text="@string/category_select" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvIntCatgHeader"
        android:contentDescription="@string/choose_interest"
        android:importantForAccessibility="auto"
    android:focusableInTouchMode="true" />
    <requestFocus />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/lvCategList"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/tvIntCatgHeader"
        android:layout_alignParentBottom="true" />

</RelativeLayout>
